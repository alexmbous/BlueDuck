<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>/-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <!-- 启动页主题 -->
    <style name="LogoTheme" parent="@android:style/Theme.Translucent.NoTitleBar"/>

    <!-- 登录提示对话框样式 -->
    <style name="myDialog" parent="android:Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--背景是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!-- 耗时请求对话框样式 -->
    <style name="requestDialog" parent="android:Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--背景是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="SelectPopupAnimation">
        <item name="android:windowEnterAnimation">@anim/select_popup_start</item>
        <item name="android:windowExitAnimation">@anim/select_popup_end</item>
    </style>

    <style name="RatingBarStyle">
        <item name="android:minHeight">40dp</item>
        <item name="android:maxHeight">40dp</item>
        <item name="android:progressDrawable">@drawable/rating_bar_bg</item>
    </style>

</resources>
